# 共享记账报销小程序 - 需求文档

## 1. 项目概述

### 1.1 项目背景

为解决团队/公司报销管理混乱、难以追踪的问题，开发一款支持多人协作的记账报销小程序。用户可创建报销账本，记录支出并标记报销状态，方便统一管理和统计。

### 1.2 目标用户

- 团队/公司财务人员
- 需要报销的团队成员
- 项目负责人

### 1.3 核心目标

1. 简化报销记录流程
2. 实时查看报销状态
3. 快速导出报销报表
4. 支持多人协作记账
5. 灵活的权限管理
6. 账本数据锁定保护

---

## 2. 功能需求

### 2.1 用户模块

#### 2.1.1 用户注册/登录
- 支持手机号+验证码登录
- 支持微信授权登录（小程序端）
- 首次登录自动创建用户账号

#### 2.1.2 个人信息管理
- 修改昵称、头像
- 绑定/更换手机号
- 查看我的账本列表

### 2.2 账本模块

#### 2.2.1 创建账本
- 账本名称（必填）
- 账本描述（选填）
- 账本类型：个人账本 / 报销账本
- 生成邀请码（用于他人加入）

#### 2.2.2 加入账本
- 通过邀请码加入
- 加入后成为普通成员

#### 2.2.3 账本列表
- 显示我创建的账本
- 显示我加入的账本
- 显示账本成员数量

#### 2.2.4 账本设置
- 修改账本信息
- 查看成员列表
- 移除成员（仅创建者）
- 删除账本（仅创建者）

#### 2.2.5 账本锁定
- **手动锁定**：管理员可随时手动锁定账本
- **自动锁定**：可配置自动锁定时间（如7天、30天、90天），超过设定时间后账本自动锁定
- **锁定效果**：锁定后，任何人（包括创建者和管理员）都无法修改或删除账目
- **解锁**：仅创建者可手动解锁账本
- **锁定状态显示**：账本详情页醒目提示当前锁定状态

### 2.3 成员管理模块

#### 2.3.1 虚拟成员
- **功能说明**：为不方便加入账本的人（如领导、客户、外部合作方）创建一个虚拟角色，记账时可选择他为收支人
- **创建虚拟成员**：
  - 名称（必填）
  - 头像（选填，可从预设头像中选择）
- **管理虚拟成员**：
  - 编辑名称和头像
  - 删除虚拟成员（无关联账目时才可删除）
- **使用场景**：
  - 记账时，收支人下拉列表同时显示真实成员和虚拟成员
  - 统计报表中虚拟成员与真实成员一起参与统计

#### 2.3.2 成员权限管理
- **角色定义**：
  - 创建者：账本所有者，拥有最高权限
  - 管理员：可管理成员和权限
  - 普通成员：基础记账和查看权限

- **权限维度**：管理员可对每个成员单独设置以下权限，每个权限支持三个级别：

  | 权限项 | 说明 | 可选值 |
  |-------|------|-------|
  | 添加账目 | 是否允许添加新账目 | 全部 / 不允许 |
  | 修改账目 | 是否允许修改已有账目 | 全部 / 仅自己 / 不允许 |
  | 删除账目 | 是否允许删除已有账目 | 全部 / 仅自己 / 不允许 |
  | 修改报销状态 | 是否允许标记报销状态 | 全部 / 仅自己 / 不允许 |
  | 导出报表 | 是否允许导出Excel | 全部 / 不允许 |

- **权限默认值**：
  - 新加入的成员默认拥有基础权限（添加账目：全部，修改/删除：仅自己，修改报销状态：仅自己，导出：全部）
  - **修改报销状态**权限默认普通管理员也没有，需创建者/管理员显式授权

- **权限说明**：
  - **全部**：可对该账本所有账目执行此操作
  - **仅自己**：仅可对自己创建的账目执行此操作
  - **不允许**：无权执行此操作

### 2.4 记账模块（核心功能）

#### 2.4.1 添加账目
- **基本信息**：
  - 金额（必填）
  - 类型：支出（默认）/ 收入
  - 分类（必填）：餐饮、交通、住宿、办公用品、其他等
  - 日期（默认当天）
  - 备注（选填）

- **收支人**：
  - 从成员列表中选择（包含真实成员和虚拟成员）
  - 默认为当前用户

- **报销信息**（仅报销账本）：
  - 报销状态：未报销 / 已报销
  - 发票/收据图片（最多6张）

#### 2.4.2 账目列表
- 按时间倒序排列
- 支持筛选：全部 / 未报销 / 已报销
- 显示账目基本信息和报销状态
- 锁定状态下，修改和删除按钮隐藏或置灰

#### 2.4.3 账目详情
- 显示完整账目信息
- 显示图片缩略图（可点击查看大图）
- 根据权限显示/隐藏修改、删除按钮
- 锁定状态下，修改和删除按钮隐藏

#### 2.4.4 修改报销状态
- 标记为"已报销"
- 标记为"未报销"
- 记录操作人和操作时间
- 受权限控制：仅拥有"修改报销状态"权限的成员可操作

### 2.5 分类模块

#### 2.5.1 分类管理
- 预设常用分类：餐饮、交通、住宿、办公用品、通讯、差旅、其他
- 支持添加自定义分类
- 支持编辑分类名称
- 支持删除分类（无账目关联时才可删除）

### 2.6 统计模块

#### 2.6.1 报销统计
- **个人统计**：
  - 我的待报销金额
  - 我的已报销金额
  - 按分类统计支出

- **团队统计**（仅报销账本）：
  - 团队总支出
  - 各成员（含虚拟成员）待报销金额
  - 各成员（含虚拟成员）已报销金额
  - 按分类统计团队支出

#### 2.6.2 时间维度
- 本月统计
- 上月统计
- 自定义时间范围

### 2.7 导出模块

#### 2.7.1 导出报销报表
- 导出格式：Excel
- 导出字段：日期、分类、金额、收支人、报销状态、备注、图片链接
- 支持按时间范围筛选导出
- 支持按报销状态筛选导出

---

## 3. 非功能需求

### 3.1 性能需求
- 页面加载时间 < 2秒
- 图片上传支持压缩（最大宽度1920px，质量0.8）
- 列表分页加载，每页20条

### 3.2 兼容性需求
- 支持微信小程序
- 支持H5网页端
- 适配主流手机分辨率

### 3.3 安全需求
- 用户数据隔离（只能查看自己参与的账本）
- 图片上传校验（仅允许jpg/png，单张最大5MB）
- HTTPS传输加密
- 权限校验在后端强制执行（前端隐藏按钮仅为体验优化，后端必须校验）

---

## 4. 页面设计

### 4.1 页面清单

| 页面路径 | 页面名称 | 说明 |
|---------|---------|------|
| pages/index/index | 首页 | 账本列表入口 |
| pages/ledger/create | 创建账本 | 新建账本页面 |
| pages/ledger/join | 加入账本 | 输入邀请码加入 |
| pages/ledger/detail | 账本详情 | 账目列表+统计Tab |
| pages/ledger/members | 成员管理 | 查看/管理成员 |
| pages/ledger/settings | 账本设置 | 修改账本信息 |
| pages/ledger/lock | 账本锁定 | 锁定设置页面 |
| pages/member/permission | 权限设置 | 成员权限配置 |
| pages/virtual/list | 虚拟成员 | 虚拟成员管理 |
| pages/virtual/edit | 编辑虚拟成员 | 添加/编辑虚拟成员 |
| pages/transaction/add | 添加账目 | 记账页面 |
| pages/transaction/detail | 账目详情 | 查看/编辑账目 |
| pages/category/list | 分类管理 | 收支分类管理 |
| pages/statistics/index | 统计报表 | 报销统计页面 |
| pages/export/index | 导出报表 | 导出Excel配置 |
| pages/mine/index | 我的 | 个人中心 |
| pages/mine/profile | 个人资料 | 修改个人信息 |

### 4.2 TabBar设计

```
┌─────────┬─────────┬─────────┐
│  账本   │  记一笔 │   我的  │
│ (首页)  │ (添加)  │ (个人)  │
└─────────┴─────────┴─────────┘
```

---

## 5. 权限设计

### 5.1 角色定义

| 角色 | 说明 |
|-----|------|
| 创建者 | 账本所有者，拥有最高权限，可管理成员、设置权限、锁定/解锁账本 |
| 管理员 | 可管理成员和设置权限，但默认没有修改报销状态权限 |
| 成员 | 基础记账和查看权限，权限由管理员配置 |

### 5.2 权限矩阵

| 功能 | 创建者 | 管理员 | 成员 |
|-----|-------|-------|------|
| 查看账本 | ✓ | ✓ | ✓ |
| 添加账目 | ✓ | 可配置 | 可配置 |
| 修改账目 | ✓ | 可配置 | 可配置 |
| 删除账目 | ✓ | 可配置 | 可配置 |
| 修改报销状态 | ✓ | 默认✗，可配置 | 可配置 |
| 管理成员 | ✓ | ✓ | ✗ |
| 设置成员权限 | ✓ | ✓ | ✗ |
| 管理虚拟成员 | ✓ | ✓ | ✗ |
| 锁定/解锁账本 | ✓ | ✗ | ✗ |
| 修改账本信息 | ✓ | ✗ | ✗ |
| 删除账本 | ✓ | ✗ | ✗ |
| 导出报表 | ✓ | 可配置 | 可配置 |

### 5.3 权限级别说明

| 级别 | 值 | 说明 |
|-----|---|------|
| 全部 | 1 | 可对所有账目执行操作 |
| 仅自己 | 2 | 仅可对自己创建的账目执行操作 |
| 不允许 | 0 | 无权执行此操作 |

### 5.4 账本锁定权限

| 状态 | 创建者 | 管理员 | 成员 |
|-----|-------|-------|------|
| 未锁定 | 正常操作 | 正常操作 | 正常操作 |
| 已锁定 | 仅查看 | 仅查看 | 仅查看 |

**锁定后限制**：
- 任何人无法添加新账目
- 任何人无法修改已有账目
- 任何人无法删除已有账目
- 任何人无法修改报销状态
- 仍可查看账目列表和统计
- 仍可导出报表

---

## 6. 数据流程

### 6.1 记账流程
```
用户进入账本 → 检查账本是否锁定 → 点击"记一笔" → 选择收支人 → 填写账目信息 → 上传凭证图片 → 保存
```

### 6.2 报销流程
```
账目列表 → 选择账目 → 检查权限 → 标记"已报销" → 记录操作时间
```

### 6.3 导出流程
```
选择时间范围 → 选择报销状态 → 生成Excel → 下载文件
```

### 6.4 账本锁定流程
```
管理员进入账本设置 → 锁定设置 → 选择锁定方式（手动/自动） → 配置自动锁定时间 → 保存
```

### 6.5 权限设置流程
```
管理员进入成员管理 → 选择成员 → 配置各项权限级别 → 保存
```

---

## 7. 原型示意

### 7.1 账本详情页
```
┌─────────────────────┐
│ 账本名称          ≡ │  ← 顶部导航
│ 🔒 账本已锁定       │  ← 锁定状态提示（仅锁定时显示）
├─────────────────────┤
│ [全部] [未报销] [已报销] │  ← 筛选Tab
├─────────────────────┤
│ 本月支出: ¥3,500    │  ← 统计卡片
│ 待报销: ¥2,000      │
├─────────────────────┤
│ 📅 2024-01-15       │
│ ┌─────────────────┐ │
│ │ 🍔 餐饮    -¥80 │ │  ← 账目列表项
│ │    未报销  张三  │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ 🚕 交通   -¥120 │ │
│ │    已报销  李四  │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ 🍜 餐饮    -¥35 │ │
│ │    未报销  王总  │ │  ← 虚拟成员
│ └─────────────────┘ │
├─────────────────────┤
│    [ + 记一笔 ]     │  ← 锁定时隐藏
└─────────────────────┘
```

### 7.2 添加账目页
```
┌─────────────────────┐
│ < 添加账目          │
├─────────────────────┤
│ 金额                │
│ ┌─────────────────┐ │
│ │ ¥              │ │
│ └─────────────────┘ │
├─────────────────────┤
│ 收支人              │
│ [张三 ▼]            │  ← 下拉选择（含虚拟成员）
├─────────────────────┤
│ 分类                │
│ [餐饮][交通][住宿]  │
│ [办公][通讯][其他]  │
├─────────────────────┤
│ 日期: 2024-01-15    │
├─────────────────────┤
│ 备注                │
│ ┌─────────────────┐ │
│ │ 请输入备注...   │ │
│ └─────────────────┘ │
├─────────────────────┤
│ 上传凭证 (0/6)      │
│ [+]                 │
├─────────────────────┤
│ [报销状态: 未报销]  │
├─────────────────────┤
│    [ 保 存 ]        │
└─────────────────────┘
```

### 7.3 权限设置页
```
┌─────────────────────┐
│ < 权限设置          │
├─────────────────────┤
│ 成员: 张三          │
│ 角色: 普通成员      │
├─────────────────────┤
│ 添加账目            │
│ [全部] [不允许]     │  ← 单选
├─────────────────────┤
│ 修改账目            │
│ [全部] [仅自己] [不允许] │
├─────────────────────┤
│ 删除账目            │
│ [全部] [仅自己] [不允许] │
├─────────────────────┤
│ 修改报销状态        │
│ [全部] [仅自己] [不允许] │
├─────────────────────┤
│ 导出报表            │
│ [全部] [不允许]     │
├─────────────────────┤
│    [ 保 存 ]        │
└─────────────────────┘
```

### 7.4 账本锁定设置页
```
┌─────────────────────┐
│ < 锁定设置          │
├─────────────────────┤
│ 锁定状态            │
│ [未锁定 ●]          │  ← 手动切换
├─────────────────────┤
│ 自动锁定            │
│ [开启 ○]            │  ← 开关
├─────────────────────┤
│ 锁定时间            │
│ [7天 ▼]             │  ← 下拉选择
│ 可选: 7天/30天/90天  │
│      /180天/永不    │
├─────────────────────┤
│ 说明：              │
│ 锁定后任何人无法    │
│ 修改或删除账目      │
├─────────────────────┤
│    [ 保 存 ]        │
└─────────────────────┘
```

---

## 8. 技术要求

### 8.1 前端技术栈
- **框架**: UniApp 3.x
- **UI组件**: uView UI / uni-ui
- **状态管理**: Pinia
- **构建工具**: Vite

### 8.2 后端技术栈
- **语言**: Node.js (Express) / Java (Spring Boot)
- **数据库**: MySQL 8.0
- **缓存**: Redis
- **文件存储**: 阿里云OSS / 腾讯云COS
- **定时任务**: node-cron（用于自动锁定检查）

### 8.3 部署环境
- **服务器**: 云服务器（2核4G）
- **域名**: 需备案域名
- **SSL**: HTTPS证书

---

## 9. 迭代计划

### 9.1 MVP版本（第一阶段）
- 用户登录/注册
- 创建/加入账本
- 基础记账功能
- 报销状态标记
- 虚拟成员管理
- 成员权限管理
- 账本锁定功能
- 基础统计

### 9.2 完整版本（第二阶段）
- 分类管理
- Excel导出
- 图片上传
- 详细统计报表

---

## 10. 附录

### 10.1 术语表

| 术语 | 说明 |
|-----|------|
| 账本 | 记账的容器，包含多个账目记录 |
| 账目 | 单条收支记录 |
| 报销状态 | 标记该笔支出是否已完成报销 |
| 邀请码 | 用于邀请他人加入账本的唯一代码 |
| 虚拟成员 | 不方便加入账本的人创建的虚拟角色，可被选为收支人 |
| 权限级别 | 全部（可操作所有账目）、仅自己（仅可操作自己创建的账目）、不允许（无权操作） |
| 账本锁定 | 锁定后任何人无法修改或删除账目，支持手动锁定和自动锁定 |

### 10.2 参考文档
- UniApp官方文档: https://uniapp.dcloud.net.cn/
- 微信小程序开发文档: https://developers.weixin.qq.com/miniprogram/dev/
